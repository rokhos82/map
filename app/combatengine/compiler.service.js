export function unitCompilerService() {
  let _service = {};

  let _keywordMap = {
    "unit.size": "info.size"
  };

  return _service;
}

unitCompilerService.$inject = [];

// Service Function Definitions Below
